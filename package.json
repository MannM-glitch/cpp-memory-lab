{
  "name": "cpp-memory-lab",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Interactive C++ pointers, references, memory and compiler optimization lab",
  "scripts": { "start": "node scripts/serve.mjs", "test": "node --test", "check": "node --check dist/app.js && node --check dist/model.js" },
  "engines": { "node": ">=22" }
}
